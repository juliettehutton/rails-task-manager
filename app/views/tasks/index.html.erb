<h2>My tasks</h2>

<ul>
  <% @tasks.each do |task| %>
    <li><%= render "form_checkbox", task: task %><%= link_to task.title, task_path(task) %></li>
  <% end %>
</ul>

<%= link_to "Create a new task", new_task_path  %>
